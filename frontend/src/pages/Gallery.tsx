const Gallery = () => {
    return (
        <main style={{ padding: '2rem' }}>
            <h1>Gallery</h1>
            <p>This is the Gallery page for Picandrew.</p>
        </main>
    );
};

export default Gallery;